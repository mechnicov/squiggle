.text-center
  %h1= @user.email
  %h3= status(@user)
  - if @user == current_user
    %p.buffer-top
      = link_to 'Редактировать профиль',                             |
        edit_user_registration_path(@user), class: 'btn btn-default' |
